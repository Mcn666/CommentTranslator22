<UserControl x:Class="CommentTranslator22.Popups.Command.Command1View"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:CommentTranslator22.Popups.Command"
             mc:Ignorable="d">
    <Grid>
        <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Border Grid.Column="0"
                Background="#FF424242">
            <TextBlock d:Text="翻译信息"
                       Text="{Binding TranslateInfo}"
                       Foreground="Wheat"
                       Margin="5"/>
        </Border>
        <Border Grid.Column="1"
                Background="#FF424242"
                MaxWidth="600">
            <TextBlock d:Text="测试文本"
                       Text="{Binding TranslateText}"
                       TextWrapping="Wrap"
                       Foreground="Wheat"
                       Margin="5"/>
        </Border>
    </Grid>
</UserControl>
